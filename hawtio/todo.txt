1. jolokia core - rebuild woth ee9
2. harwtio

 root pom:
 1.
 
     <maven.compiler.source>17</maven.compiler.source>
    <maven.compiler.target>17</maven.compiler.target>
 
 2.
    <servlet-api-version>6.0.0</servlet-api-version>
	<jolokia-version>1.7.3-SNAPSHOT</jolokia-version>
	<!-- Spring versions -->
    <!--
    <spring-version>5.3.27</spring-version>
    <spring-boot-version>2.7.12</spring-boot-version>
    -->
    <spring-version>6.0.11</spring-version>
    <spring-boot-version>3.1.3</spring-boot-version>
    <!-- Quarkus version -->
    <quarkus-version>3.1.2.Final</quarkus-version>
    <!--
    <quarkus-version>2.16.6.Final</quarkus-version>
    -->
	
3. module
    <module>bom</module>
    <module>console</module>
    <module>hawtio-system</module>
    <module>platforms/springboot</module>
    <module>plugins/hawtio-log-logback</module>
    <module>plugins/hawtio-log</module>	
 
4. hawt-system 
    <dependency>
      <groupId>jakarta.servlet</groupId>
      <artifactId>jakarta.servlet-api</artifactId>
      <version>${servlet-api-version}</version>
      <scope>provided</scope>
    </dependency>
	
	replace javax with jakarta for servlet api
	
	
	FlightRecordingDownloadFacade: 184
	        // @SuppressWarnings("deprecation")
        // @Override
        // public String getRealPath(String path) {
        //     return replaceInPath(super.getRealPath(path));
        // }
	proxy servlet 283:
		     // servletResponse.setStatus(statusCode, proxyResponse.getStatusLine().getReasonPhrase());
            // TODO
            servletResponse.setStatus(statusCode); //, proxyResponse.getStatusLine().getReasonPhrase());
 5. hawtio-springboot

    replace javax with jakarta

    org.springframework.boot.actuate.autoconfigure.jolokia classes

    resources:
	    META-INF/spring
		org.springframework.boot.actuate.autoconfigure.web.ManagementContextConfiguration.imports
		org.springframework.boot.autoconfigure.AutoConfiguration.imports
		
		io/hawt/springboot
		application.properties
		
	